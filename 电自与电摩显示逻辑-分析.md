# 电自/电摩在 APP 内的显示逻辑 — 分析报告

## 1. 现象与目标

| 场景 | 电自 | 电摩 |
|------|------|------|
| **仪表盘** | 只显示 W/%（功率、电量） | 显示时速等 |
| **轨迹记录** | 只显示时间、耗电量 | 显示详情（如轨迹详情、里程、时速等） |

目标：找到**控制型号（电自/电摩）的代码位置**，以及**可通过 Hook 修改以实现“按电摩显示”的参数**。

---

## 2. 型号控制核心：`vehicleType`（车辆类型）

### 2.1 数据源

- **类**：`cn.ninebot.device.bean.DeviceBean`
- **字段**：`vehicleType`（int，默认 -1）
  - JSON 序列化名：`"vehicle_type"`（来自服务端设备列表/详情接口）
- **方法**：
  - `getVehicleType()` / `setVehicleType(int)`
  - `getModel()`：用 `vehicleType` 在 `NbBluetoothClient.getSupportDeviceModels()` 中匹配 `DeviceModel.getId() == vehicleType`，得到当前设备的 `DeviceModel`

即：**全 APP 内“当前是什么车型”的统一来源是 `DeviceBean.vehicleType`**。

### 2.2 vehicleType 的写入来源

| 来源 | 位置 | 说明 |
|------|------|------|
| 服务端 API | 设备列表/详情 JSON 中的 `vehicle_type` 字段 | 反序列化进 `DeviceBean.vehicleType` |
| 本地 DB | `cn.ninebot.device.DeviceManager.getTrackingDevice()` | 从 `Device` 取 `getDeviceType()` 调用 `deviceBean.setVehicleType(deviceQueryBySN.getDeviceType())` |
| BLE 设备 | `cn.ninebot.device.NbBluetoothDeviceEtKt.createDeviceBean(NbBluetoothDevice)` | `deviceBean.setVehicleType(deviceModel != null ? deviceModel.getId() : -1)` |

结论：**只要统一改“读到的 vehicleType”，仪表盘和轨迹等都会按新类型走**。因此 Hook 应放在**读取**侧，而不是每个业务单独改。

---

## 3. 仪表盘显示（W/% vs 时速）

- **相关 UI**：
  - 车控/详情页头部卡片：`cn.ninebot.device.widget.detail.ScooterDetailHeadCardView`（继承 `DynamicDetailHeadCardView`）
  - 当前看到的是：电量%、续航里程等；**是否显示“时速”很可能由动态配置或根据 `DeviceModel`/vehicleType 在别处控制**（如动态配置、不同 layout、或 RN 仪表页）。
- **逻辑链**：
  - 详情/仪表相关页面会通过 `DeviceManager.getCurrentDeviceBean()` 或 `DeviceProvider.getCurrentVehicleType()` 拿到当前车。
  - `DeviceProviderImpl.getCurrentVehicleType()` 实现为：  
    `DeviceBean current = DeviceManager.getCurrentDeviceBean(); return current != null ? current.getVehicleType() : null;`
- **结论**：仪表盘“只显示 W/%”还是“显示时速”由**当前车的 vehicleType（或由其得到的 DeviceModel）** 决定。**Hook `DeviceBean.getVehicleType()` 或 `DeviceProviderImpl.getCurrentVehicleType()`，让电自返回“电摩”的 type，即可让仪表按电摩逻辑显示。**

---

## 4. 轨迹记录显示（仅时间/耗电 vs 详情）

- **轨迹录制页**：`com.ninebot.track.ui.TrackFragment`
  - 通过 Arguments 传入 `sn`、`EXTRA_VEHICLE_TYPE`（即 vehicleType）。
  - 传入处：`com.ninebot.track.TrackProviderImpl.trackView(activity, containerId, deviceSn, vehicleType)`，其中 **vehicleType 由调用方传入**；调用方一般从 `DeviceProvider.getCurrentVehicleType()` 或当前 `DeviceBean.getVehicleType()` 取得。
- **轨迹列表/详情**：
  - 录制结束后通过 `RNProvider.toOtherTrackDetail(json)` 等跳转到 RN 或其它页面；列表/详情是否“仅时间+耗电”还是“完整详情”很可能也根据**同一条轨迹关联的 vehicleType** 或服务端下发的配置区分。
- **结论**：轨迹相关逻辑同样依赖 **vehicleType**。**统一把“当前设备/轨迹”的 vehicleType 在读取时改为电摩类型，即可让轨迹也按电摩的“详情”方式展示。**

---

## 5. Hook 方案建议

### 5.1 推荐：Hook 读取 vehicleType 的出口

**方案 A（推荐）**：Hook **`cn.ninebot.device.bean.DeviceBean.getVehicleType()`**

- **时机**：`afterHookedMethod`
- **逻辑**：若配置项为“强制按电摩显示”，则对返回值做映射：  
  - 若当前返回值属于“电自”类型（需先确认具体 int 值，见下），则 `param.setResult(电摩的 type 值)`；否则不改。
- **优点**：一处修改，所有通过 `DeviceBean.getVehicleType()` 拿类型的地方（仪表、轨迹入口、RN、动态配置等）都会生效。

**方案 B**：Hook **`cn.ninebot.device.DeviceProviderImpl.getCurrentVehicleType()`**

- **时机**：`afterHookedMethod`
- **逻辑**：若配置为“强制按电摩显示”，则 `param.setResult(电摩的 type)`（或按需映射）。
- **优点**：只影响“当前选中的设备”的类型，不影响其他设备；若轨迹/仪表都从 getCurrentVehicleType 取，则同样一处生效。

**方案 C**：Hook **`com.ninebot.track.TrackProviderImpl.trackView(..., int vehicleType)`**

- **时机**：`beforeHookedMethod`，修改 `param.args[3]`（vehicleType 参数）为电摩 type。
- **优点**：只改轨迹录制/展示用的类型，不动全局设备类型。
- **缺点**：仪表盘若不用 trackView 的入参，则需另 Hook（如方案 A/B）。

建议**优先实现方案 A 或 B**，再视需求是否对轨迹单独再 Hook（方案 C）。

### 5.2 电自 / 电摩的 type 值（需实测确认）

- 代码中**未发现**名为“电自”“电摩”的常量；`vehicleType` 为 int，与 `DeviceModel.getId()` 一致，具体取值依赖：
  - 服务端下发的 `vehicle_type`
  - 或本地 `NbBluetoothClient.getSupportDeviceModels()` 里各 `DeviceModel.getId()`
- **建议**：
  1. 抓包设备列表/详情接口，看 `vehicle_type` 在电自/电摩上的取值；或  
  2. 在 `DeviceBean.getVehicleType()` 上打日志，分别连接电自、电摩看返回值；或  
  3. 查本地 DB 表（如 `Device`）中 `device_type` 字段，对比电自/电摩。

确定后，在 Hook 里用“电自 type → 电摩 type”的映射即可。

---

## 6. 小结

| 项目 | 结论 |
|------|------|
| **型号在哪里控制** | `DeviceBean.vehicleType`（int），来自 API、本地 DB、BLE 的 DeviceModel.getId() |
| **仪表盘 W/% vs 时速** | 由当前设备的 vehicleType/DeviceModel 决定；改 getVehicleType 或 getCurrentVehicleType 可改为电摩显示 |
| **轨迹 简版 vs 详情** | 由传入轨迹的 vehicleType 决定；来源同样是当前 DeviceBean 或 getCurrentVehicleType；同一 Hook 可兼顾 |
| **推荐 Hook 点** | `DeviceBean.getVehicleType()`（方案 A）或 `DeviceProviderImpl.getCurrentVehicleType()`（方案 B） |
| **待确认** | 电自、电摩对应的 vehicleType 整型值（抓包/日志/DB 确认后再写死或配置化） |

实现时可在插件中增加配置项（如“强制按电摩显示”开关），仅当开启时对 getVehicleType/getCurrentVehicleType 做上述映射，并预留配置“电摩 type 值”或“电自→电摩映射表”，便于后续根据实测值填写。

---

## 7. 插件已实现（当前仓库）

- **配置项**（SharedPreferences `theme_config`）：
  - `enable_force_motor_display`：是否开启“电自按电摩显示”
  - `force_motor_vehicle_type`：强制使用的 vehicleType 整型值（默认 2，需根据抓包/日志确认后调整）
- **Hook 点**：`cn.ninebot.device.bean.DeviceBean.getVehicleType()`  
  - 当 `enable_force_motor_display == true` 时，返回值被替换为 `force_motor_vehicle_type`。
- **MainActivity**：新增「5. 电自按电摩显示」开关 + 电摩 vehicleType 输入框；保存设置时一并写入上述两项。
- **vehicleType 位数**：整数，位数不固定（常见 1～3 位，如 0、2、116 等），具体以接口/日志为准。示例：九号电动Dz110P（电自）抓包为 **vehicle_type=116**。**注意**：勿填未验证数值，如 **117** 会触发内部体验逻辑（「发起导航(体验版)」「仅支持高德自行车」、发起后一直转圈），仪表盘仍为 W/%。
- **为何 117、118 都出现「发起导航(体验版)」「仅支持高德自行车」**：JADX 确认控制链是 **DeviceBean.getVehicleType()** → **DeviceBeanExtKt.getConfig(deviceBean)** → **DynamicConfigManager.getDeviceConfigWithServerId(deviceBean.getVehicleType())**。动态配置按 vehicleType（serverId）拉取；APP 里只预置了部分车型（如 116）的配置。当 vehicleType 改为 117、118 等**未配置的 ID** 时，getConfig 得到 **null 或默认配置**，导航逻辑会走**同一套“未知车型”分支**（体验版 + 高德自行车），所以不是“两个碰巧都是内部功能”，而是**凡是没有对应动态配置的 type 都会落进这一分支**。

- **上送仍用官方 116（单机本地版）**：修改车辆型号仅用于**本地**仪表盘、轨迹展示；车辆上送信息按官方 116 处理。实现方式：Hook `getVehicleType()` 时从 `DeviceBean` 的 **vehicleType 字段**读取真实值；当调用栈包含网络/上送相关类（如 retrofit、okhttp、nbnet、encrypt、gson、toJson 等）时返回该真实值，其余场景（本地 UI、getConfig、导航/轨迹入口等）返回配置的展示用 type。这样服务端收到的仍是官方车型信息。

- **正确流程（不要先开「电自按电摩显示」）**：  
  1. 先**不要**开启「电自按电摩显示」。  
  2. 只开启「**其他**」，配置 Web 日志服务器并保存。  
  3. 打开九号 APP，进设备列表或车辆详情，触发接口。  
  4. 在 Web 日志里看 tag **车辆信息**，会明文看到当前车型的 `vehicle_type`、`wnumber` 等。  
  5. 把当前车型的 `vehicle_type` 填到插件「电摩 vehicleType」里**作为默认值**（当前是电摩就得到电摩值；是电自可再换电摩抓一次）。  
  6. 确认数值后再开启「电自按电摩显示」测试。
