# 九号 APP「我的」与主题付费判断 — MCP 分析报告

基于 JADX MCP 对脱壳后的九号 APP 的分析，说明「我的」入口下「正在使用的内容」「我的主题库」「查看订单」与**已付费购买主题**判断的大致实现方式。

---

## 1. 结论概览

- **「我的」页的 UI 与业务逻辑不在原生 Java 里实现**，而是在 **React Native** 里实现（模块名 `CustomSoundEffect`，路由名 `Theme`）。  
- **是否展示「主题」Tab、是否进入带主题的详情页** 由原生层通过云控配置（`themeShow` / `TopGuideTabConfig`）控制；  
  **是否已付费、我的主题库、查看订单** 的**判断与展示**由 **RN 页面 + 后端接口** 完成，原生层没有对应的订单/购买 API。

---

## 2. 原生层与主题相关的部分

### 2.1 主题 Tab 的容器：ThemeFragment

- **类**：`cn.ninebot.device.theme.ThemeFragment`
- **继承**：`NBReactLazyFragment<FragmentThemeBinding>`
- **作用**：只做「壳」——加载布局 `R.layout.fragment_theme`，并把**实际内容交给 React Native**：
  - `getModuleName()` → **"CustomSoundEffect"**
  - `getRouteName()` → **"Theme"**
  - `getParams()` → **空 Bundle**，没有把「是否购买」等从原生传进去。

因此，你在界面上看到的「我的」「正在使用的内容」「我的主题库」「查看订单」等，都是 **RN 里画的**，不是 ThemeFragment 里写的。

### 2.2 原生与 RN 的通信

- ThemeFragment 通过 **`sendEventToReactNative("switchThemeSubTab", writableMap)`** 向 RN 发事件（用于子 Tab 切换等）。
- **没有**在原生层发现：把「已购买主题列表」或「订单信息」通过事件/params 传给 RN 的逻辑。  
  即：**「已付费」相关的数据不是由 ThemeFragment/原生先算好再给 RN，而是 RN 自己请求接口拿的。**

### 2.3 原生侧与主题相关的接口（TopGuideApiService）

在 `cn.ninebot.device.network.TopGuideApiService` 中，与主题相关的只有三个接口：

| 接口 | 路径 | 返回/作用 |
|------|------|------------|
| getTopGuideConfig | `POST /app-api/theme/v1/batch-show-config` | `EntranceResult` → `showEntranceList`（List&lt;TopGuideTabConfig&gt;），每项含 **themeShow / vipShow**，用于控制是否显示「主题」Tab、是否走 TopGuide 详情页。 |
| getThemeVersion  | `POST /app-api/theme/v1/get-new-version`   | `ThemeVersionBean`（showRedDot），用于主题「新版本」红点。 |
| clearThemeRedBadge | `POST /app-api/theme/v1/cancel-reddot`   | 清除主题红点。 |

- **EntranceResult / TopGuideTabConfig**：只包含 **wnumber, themeShow, vipShow**，表示「是否给用户展示主题入口」，**不包含**「用户是否已购买某主题」「订单列表」等。
- 在**整个九号包名下的 Java 代码**中，**未发现**与「订单 / 购买 / 支付 / 我的主题库」相关的接口定义（如 order、purchase、pay、my theme 等）。

因此可以确定：  
**「已经付费购买过主题」的判定，不是由上述原生接口完成的，而是由 RN 调用的其它接口（或同一域下的其它 path）完成的。**

---

## 3. 「我的主题库」与「查看订单」的实现位置（推断）

- **界面**：由 RN 模块 **CustomSoundEffect**、路由 **Theme** 渲染；点击「我的」后出现的「正在使用的内容」「我的主题库」「查看订单」都在该 RN 页面内。
- **数据来源**：  
  - 要么 RN 请求了**未在 TopGuideApiService 中声明的**其它接口（例如 `/app-api/theme/...` 下其它 path，或订单/商品相关接口）；  
  - 要么请求的是同一批接口，但服务端根据用户身份返回「已购主题列表」等，再由 RN 解析并展示「我的主题库」「查看订单」。

**判断「已经付费购买过主题」的典型流程（逻辑上）**：

1. 用户点击「我的」→ 进入 RN 的 Theme 页面。
2. RN 请求「我的主题 / 已购主题 / 订单列表」等接口（具体 path 需抓包确认）。
3. 服务端根据登录态返回：已购主题 ID 列表、或订单列表、或带「已拥有」标记的主题列表。
4. RN 根据返回结果展示「正在使用的内容」「我的主题库」和「查看订单」；若某主题在「已购」列表中，则显示为已购买。

原生层**没有**参与「是否已付费」的判断，只负责：是否展示主题 Tab（themeShow）、是否进 TopGuide 详情、以及把主题页的容器交给 RN。

---

## 4. 如何进一步确认「付费判断」与接口

1. **抓包**  
   - 在手机/模拟器上打开九号 → 进入「主题」Tab → 点击「我的」→ 再进「我的主题库」或「查看订单」。  
   - 用 Charles / Fiddler / 你现有的抓包（或插件里的 Http 抓包）看这段时间内的请求：  
     - 若有 `/app-api/theme/...` 或 `/order/...`、`/product/...`、`/user/theme/...` 等，记下 URL 和响应结构（例如是否包含 `owned`、`purchased`、`orderList` 等字段）。
2. **看 RN  bundle**  
   - 若能从安装包或运行目录拿到 React Native 的 JS bundle，可搜索「我的主题库」「查看订单」「订单」等字符串或可能的接口 path，定位到具体请求与判断逻辑。
3. **你当前插件的价值**  
   - 插件已 Hook 的 **NeteaseDecrypt.decodeContent**、**TopGuideConfigRepository.getTopGuideTabConfig**、**TopGuideDeviceDetailViewModel.updateTabList** 等，只影响「是否展示主题 Tab、是否进详情页、Tab 列表里有没有主题」；  
   - **不会**直接改变「是否已付费」的接口返回；若要模拟「已购买」，需要在 RN 调用的那套接口或返回数据上做文章（例如 Hook 该接口的返回，或改 RN 收到的数据），这需要先通过抓包/逆向 RN 确定接口与字段。

---

## 5. 小结表

| 能力 | 实现位置 | 说明 |
|------|----------|------|
| 是否显示「主题」Tab、是否进入 TopGuide 详情 | 原生（themeShow / isUseTopGuideMode / updateTabList） | 已由当前插件 Hook 控制 |
| 「我的」「正在使用的内容」「我的主题库」「查看订单」界面 | RN（CustomSoundEffect，Theme） | 原生 ThemeFragment 只提供 RN 容器 |
| 是否已付费购买主题、我的主题库列表、订单 | RN + 后端接口（未在 TopGuideApiService 中发现） | 需抓包/逆向 RN 确定具体接口与判断逻辑 |

以上为基于当前 JADX 脱壳代码与 MCP 分析得到的**工作原理说明**；具体接口 path 与字段名以抓包和 RN 逆向结果为准。
