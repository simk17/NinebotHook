# 九号 Hook：反调试与功能精简 — 可行性报告与计划

## 一、目标

1. **反调试**：在模块内干掉/绕过九号 APP 的调试检测，使 Frida 可正常附加、抓特征、改逻辑。
2. **功能精简**：移除当前「主题 Tab 强制开启」与「仪表修改」相关代码，模块只保留反调试 + 基础注入（Toast/水印/日志），为后续用 Frida 精准分析铺路。

---

## 二、反调试现状与计划

### 2.1 当前已实现

| 检测项 | 处理方式 | 位置 |
|--------|----------|------|
| `Debug.isDebuggerConnected()` | Hook 后强制返回 `false` | `NinebotHook.hookDebuggerAndReport()`，在类加载时即 Hook |

该 Hook 在**非 zygote 进程**内生效，可避免 APP 因「检测到调试器」直接退出。

### 2.2 常见 Android 反调试（需逐一确认是否存在于九号）

| 类型 | 说明 | 建议在模块中的处理 |
|------|------|---------------------|
| **Java 层** | `android.os.Debug.isDebuggerConnected()` | ✅ 已 Hook |
| **/proc/self/status** | 读 `TracerPid` 非 0 则认为被调试 | 在九号进程内 Hook `FileInputStream.read` / 或 native 读该文件的逻辑；或 Hook `Runtime.exec("cat /proc/self/status")` 等，将返回内容里 `TracerPid:\t0` 保持不变/替换。需 JADX 搜 `TracerPid`/`/proc/self` 确认。 |
| **ptrace(PTRACE_TRACEME)** | native 里防止被 attach | 需在 so 里 Hook `ptrace`，或 Frida 的 anti-anti-debug 脚本在 native 层处理。 |
| **Frida 特征** | 检测 frida-server、默认端口 27042、库名等 | 不改端口则易被扫到；可 Frida 脚本里隐藏端口/改名，或模块不直接管，先靠关掉 isDebuggerConnected + TracerPid 看能否跑 Frida。 |
| **时间检测** | 调试时单步导致时间异常 | 一般 Frida 不单步则影响小；若 APP 有可再针对性 Hook。 |

### 2.3 你需要做的（手机端）

1. **安装 Frida 环境**
   - 手机：root + 安装 **frida-server**（与电脑端 frida-tools 版本一致，如 16.x）。
   - 电脑：`pip install frida-tools`，用 `frida -U -f cn.ninebot.ninebot -l script.js --no-pause` 等启动。
2. **验证顺序**
   - 先**只开 LSPosed 模块（反调试）**，不附加 Frida，确认九号正常启动、无「检测到调试器」。
   - 再**附加 Frida**（spawn 或 attach），看是否仍被踢或闪退；若会，再根据日志/行为在 JADX 里搜 TracerPid、ptrace、debug、frida 等，在模块或 Frida 脚本里补 bypass。

### 2.4 模块侧后续可增强（按需）

- **TracerPid**：若确认九号有读 `/proc/self/status`，可在模块内 Hook 读该文件的调用，将返回流中的 `TracerPid:\t<非0>` 改为 `TracerPid:\t0`（或只读时替换）。
- **不新增 native 依赖**：ptrace/Frida 特征尽量用 Frida 脚本在 native 层解决，模块保持 Java/Xposed 层，便于维护。

---

## 三、主题 / 仪表功能移除范围

### 3.1 仪表

- **结论**：本仓库**没有**仪表修改的实现代码；《九号骑行轨迹与仪表-关键信息.md》仅为 JADX 分析笔记（如 `SmallSpeedDashView`、`DashSpeedViewController`、`isPercent`）。
- **操作**：无需删代码；若希望文档与「只做反调试」一致，可后续把该文档移出或标注为「待 Frida 分析后再实现」。

### 3.2 主题（需全部移除）

涉及文件与改动要点如下。

| 文件 | 移除/修改内容 |
|------|----------------|
| **NinebotHook.java** | ① 删除 `sThemeHookDone`、`logThemeConfigState`、`queryProviderAndLog`；② 删除整块「主题 Hook」逻辑：`hookThemeTabConfig`、`hookThemeApiRequest`、`hookIsUseTopGuideMode`、`hookUpdateTabList`、`fakeThemeShow1`、`newConfig`、`ensureThemeHookOnce`、`getNinebotClassLoaderFromActivityThread`；③ `handleLoadPackage` 内不再根据包名/processName 执行主题 Hook，不再调用 `ensureThemeHookOnce`；④ Application.attach 里只保留 Toast，去掉 `ensureThemeHookOnce`；⑤ onResume 水印回调里去掉 `logThemeConfigState`、`ensureThemeHookOnce` 及主题相关状态日志；⑥ 保留：`hookDebuggerAndReport`、`hookApplicationAttachForToast`、`hookWatermark`、`hookAllHttpResponses`（可选，抓包用）、`getAppContext`。 |
| **HookConfig.java** | ① 删除主题专用常量：`FILE_THEME`、`NINEBOT_LOCAL_THEME`；② 删除方法：`themeFileInNinebot`、`themeFile()`、`isThemeForceEnabled`、`writeThemeToNinebotLocal`、`readThemeFromXSharedPreferences`（仅 theme_force）、`readThemeFromProvider`、`isThemeFromProvider`、`setThemeForceEnabled`；③ 保留 `XPREFS_THEME`（仍给 debug_overlay、screencast 用）；④ 其余配置（debug_overlay、screencast、server 等）保留。 |
| **ConfigProvider.java** | ① 删除与 theme 相关的 URI、`readThemeFile`、`writeThemeFile`、theme 的 query/type 实现；② 若 Provider 仅剩其它键，保留其余；若仅 theme 则需保留 ContentProvider 空壳或最小实现，避免调用处 NPE。 |
| **MainActivity.java** | ① 删除「强制开启主题」整行：Label + Switch + `HookConfig.isThemeForceEnabled` / `setThemeForceEnabled`；② 保留其它开关（如 debug_overlay、screencast 提示等）。 |
| **AndroidManifest.xml** | 仅当存在 theme 相关 meta/Provider path 时删除；当前 `xposedsharedprefs` 的 `theme_config` 为 SharedPreferences 名，与 debug_overlay 等共用，可保留。 |

移除后，模块行为应为：**仅做反调试（isDebuggerConnected）+ 注入提示（Toast）+ 水印 + 可选 HTTP 日志**，不再读写主题配置、不再 Hook TopGuide/云控/updateTabList。

---

## 四、实施计划（建议顺序）

1. **反调试**
   - 保持现有 `Debug.isDebuggerConnected` Hook 不变。
   - 你在手机安装 Frida、用当前模块验证：先不 attach Frida 看 APP 是否正常；再 attach 看是否被检测。若被检测，把现象（如弹窗文案、logcat）与 JADX 里搜到的检测点反馈，再在报告里补 TracerPid/ptrace 等方案并实现。

2. **移除主题相关代码**
   - 按第三节表格在 `NinebotHook.java`、`HookConfig.java`、`ConfigProvider.java`、`MainActivity.java` 中删除/简化，保证编译通过、LSPosed 勾选后九号能正常启动、Toast/水印仍存在、不再有主题相关逻辑。

3. **仪表**
   - 不删代码；仪表相关以后若要做，可在 Frida 里先抓 `DashSpeedViewController`、`isPercent` 等特征，再决定是否在模块里加 Hook。

4. **后续**
   - 用 Frida 在九号里抓特征、改逻辑；需要时再在模块内增加**针对性** Hook（基于 Frida 验证过的调用链）。

---

## 五、风险与注意

- **反调试**：若 APP 有多层/ native 检测，可能需多轮「Frida 附加 → 被踢 → JADX 查检测点 → 模块或 Frida 脚本补 bypass」。
- **主题移除**：`ConfigProvider`、`HookConfig` 里与 XSharedPreferences 的 `theme_config` 名仍被 debug_overlay/screencast 使用，删除主题时不要删掉 `XPREFS_THEME` 常量名及该 SharedPreferences 的读写路径，只去掉 key 为 `theme_force` 的读写与主题专用文件/Provider URI 即可。

以上为完整分析与计划，可按第四节顺序执行；反调试若遇新检测点再补充到第二节表格并实现。

---

## 六、已完成（代码层面）

- **主题相关已全部移除**：NinebotHook.java（主题 Hook/延后/云控/updateTabList/isUseTopGuideMode/getNinebotClassLoader 等）、HookConfig.java（主题开关读写/Provider 读主题）、ConfigProvider.java（theme URI 与读写改为空壳）、MainActivity.java（主题开关 UI 已删）。
- **仪表**：仓库内无仪表修改代码，无需删除。
- **当前模块行为**：仅反调试（`Debug.isDebuggerConnected` → false）+ Toast 注入提示 + 水印 + 可选 HTTP 抓包（RetrofitStrategy）；版本号 0.35（插件v35）。
