# 九号出行 - 投屏导航工作流程分析

## 1. 结论概览

- **蓝牙（BLE）**：负责 **连接车辆、拉取投屏配置、下发导航指令/状态**；在“仅 BLE”模式下也负责 **画面数据** 传输（RTP over BLE）。
- **WiFi（Wifi P2P）**：负责 **与车机建立 P2P 连接**；在“WiFi 通道”下负责 **画面/视频流** 传输（RTP over TCP 或 KCP）。
- 通道由 **车端下发的投屏配置**（`ScreenCastConfig.channel`）决定，支持 **BLE / WIFI / WIFI_KCP** 三种。

---

## 2. 整体流程（谁发起、顺序）

### 2.1 入口

| 入口 | 说明 |
|------|------|
| `NaviHelperKt.startNavi()` | 通用“开始导航”入口（参数含 Context、DeviceBean、是否投屏等） |
| `DashNaviDataMessenger.Companion.prepareScreencast()` | 投屏前准备：权限、连接 WiFi（若需要）、prepare |
| `DashNaviDataMessenger.Companion.startScreenCast()` | 真正拉起投屏（内部调 `DeviceScreenCastManager` 相关逻辑） |
| `ScreenCastHelper` | 持有 `DeviceScreenCastManager`，实现 `DeviceScreenCastListener`，`startScreenCast()` 里做 prepare → build → 创建导航投屏 |

即：**用户点“投屏导航” → 先 prepare（可选连 WiFi）→ 再 build/startScreenCast → 开始发导航指令 + 发画面**。

### 2.2 顺序（简要）

1. **BLE 已连接**：车辆通过 BLE 已与 APP 连接（`NbBluetoothDevice` / `DynamicDevice`）。
2. **准备阶段 prepare**：  
   - 若为 WiFi 通道：先申请 Wifi P2P 权限，再 `DeviceScreenCastManager.prepare(context, boardId, refreshWifiInfo, wifiTimeout)`。  
   - prepare 内部会通过 **BLE** 拉取车端投屏配置（见下），并根据配置决定是否 `connectWifi`，从而得到 `dataSender`（BLE 或 WiFi）。
3. **构建 build**：  
   - `DeviceScreenCastManager.build()` → `ScreenCastRequestBuilder.config()`（可能再通过 BLE 读 config）→ `build(ScreenCastConfig, dataSender)`。  
   - 若未传入 `dataSender`，则按 `ScreenCastConfig.communicateChannel()` 创建：**BLE** 用 `ScreenCastDataSender(device, …)`（内部 BLE RTP），**WIFI/WIFI_KCP** 用 WiFi 的 `ScreenCastDataSender` + `WifiP2PInfo`（IP/Port）。
4. **开始投屏**：  
   - 创建 `ScreenCastRequest`（`ScreenCastManager.newRequest(config, dataSender, layoutInflater)`），并 `createNavigationScreenCast` 等，开始 **画面采集 + 编码 + 通过 BLE 或 WiFi 发送**。  
   - 同时 **导航指令** 始终通过 **BLE** 由 `DashNaviDataMessenger` 下发（如 `setNaviStart`、`setNaviInfo`、`setNaviRoad` 等）。

---

## 3. 蓝牙（BLE）负责什么

- **连接与设备**：与车机建立 BLE 连接，得到 `NbBluetoothDevice` / `DynamicDevice`。
- **投屏配置**：  
  - `ScreenCastConfigClient.getConfig(context, …)` 通过 **BLE 读车端文件**：`device.startReadFile(fileType, board, …)`（`BtFileReader` / `BtFile`）。  
  - 配置为 JSON，含 `channel`、`rtpChannel`、编码参数、layout 等；解析为 `ScreenCastConfig`。  
  - 即：**“用哪种通道（BLE/WIFI/WIFI_KCP）、编码格式、分辨率等”均由车端通过 BLE 下发**。
- **导航指令与状态（基础导航）**：  
  - `DashNaviDataMessenger` 通过 BLE 发送指令：  
    - `setNaviStart`、`setNaviInfo`、`setNaviDistance`、`setNaviRoad`、`setNaviRoadNext`、`setNaviText`、`setNaviDriveInfo`、`setNaviExit` 等。  
  - 并接收车端上报：如 `rAppNaviState`、仪表主动退出（tftExitCommand）等。  
  - 这些与“路线、当前路名、下一路名、距离、播报文本”等强相关，即你说的 **“基础导航”**。
- **仅 BLE 通道时的画面**：  
  - 当 `ScreenCastConfig.communicateChannel()` 为 **BLE** 时，画面也走 BLE：`ScreenCastDataSender` 使用 `BluetoothRtpSender(IBleSender, bleMtu)` 发送 RTP 数据。  
  - BLE 断开会触发 `reconnectBle()`，并 stop 当前 dataSender。

---

## 4. WiFi 负责什么

- **与车机建链**：  
  - `DeviceScreenCastManager.connectWifi(context, refreshWifiInfo, timeout)` → `NBWifiClient.connectWifiP2P(...)`。  
  - 使用 **Wifi P2P**（`WifiP2PClient`）与车机建立连接，得到 `WifiP2PInfo`（IP、Port）。
- **画面传输（WiFi 通道）**：  
  - 当配置为 **WIFI** 或 **WIFI_KCP** 时，`ScreenCastDataSender` 使用 `RtpSender`（TCP 或 KCP）向车机 **IP:Port** 发送编码后的画面数据。  
  - `resumeWifiVideoData()` 仅在当前为 WiFi 通道时恢复发送（`screenCastDataSender.resume()`）。
- **断线与重连**：  
  - 实现 `WifiP2PClient.DisconnectListener.onDisconnect()`：WiFi P2P 断开时停止发数据、清理状态。  
  - `reconnectWifi(context)`：重新 `connectWifiP2P`，成功后用新 `WifiP2PInfo` 对现有 `ScreenCastDataSender` 做 `create(channelConfig)` + `start()`。  
  - 若之前是 BLE 通道，则 BLE 断开只 `reconnectBle()`，不建 WiFi。

---

## 5. 通道枚举与配置来源

- **CommunicateChannel**（`cn.ninebot.screencast`）：  
  - `BLE`  
  - `WIFI`  
  - `WIFI_KCP`  
  - `isWifiChannel()` = true 当且仅当为 WIFI 或 WIFI_KCP。
- **ScreenCastConfig**：  
  - 字段含 `channel`、`rtpChannel`、`rtcpChannel`（字符串，与枚举序数对应）、编码相关（codecName、codecType、frame_rate、bit_rate、layout 等）。  
  - `communicateChannel()` 实际用 `rtpChannel()`，再 fallback 到 `channel()`，解析为枚举；默认 **BLE**。
- 配置来源：**仅来自车端**。APP 通过 **BLE** 用 `ScreenCastConfigClient` 读车端文件得到，或使用本地缓存（若已存过）。

---

## 6. 关键类与职责

| 类 | 职责 |
|----|------|
| `ScreenCastManager` | 单例；根据 `ScreenCastConfig.communicateChannel()` 创建 BLE 或 WiFi 的 DataSender，并 `newRequest(config, dataSender, layoutInflater)` 得到 `ScreenCastRequest`。 |
| `DeviceScreenCastManager` | 按设备管理投屏：prepare、build、connectWifi、disconnectWifi、reconnectWifi、reconnectBle、resumeWifiVideoData、isWifiChannel、监听 BLE 断开/WiFi P2P 断开。 |
| `DeviceScreenCastRequest.ScreenCastRequestBuilder` | config（可走 BLE 读配置、可选执行 wifiP2PBlock 拿 WifiP2PInfo）→ build(config, sender)；若 sender 为 null 则按 config 的 channel 创建 BLE 或 WiFi 的 `ScreenCastDataSender`。 |
| `ScreenCastConfigClient` | 通过 BLE 从车机读投屏配置文件，解析为 `ScreenCastConfig`。 |
| `DashNaviDataMessenger` | BLE 侧导航：发送/接收上述导航指令与状态，与仪表联动。 |
| `ScreenCastHelper` | 持有 `DeviceScreenCastManager`，实现 `DeviceScreenCastListener`，在导航/投屏流程中调用 prepare、build、startScreenCast 等。 |
| `ScreenCastDataSender` | 封装 BLE 或 WiFi 的发送端（BluetoothRtpSender 或 RtpSender），负责实际 RTP/画面数据发送；支持 create(channelConfig)、start、stop、pause、resume。 |

---

## 7. 数据流小结

| 数据类型 | 方向 | 通道 |
|----------|------|------|
| 投屏配置（channel、编码、layout 等） | 车机 → APP | BLE（读文件） |
| 导航指令与状态（起停、路名、距离、播报等） | APP ↔ 车机 | BLE |
| 画面/视频流 | APP → 车机 | BLE 或 WiFi（由 config 的 channel 决定） |

因此：**“蓝牙是基础导航，WiFi 是画面传输”** 在“通道选为 WIFI/WIFI_KCP”时成立；若通道选为 BLE，则基础导航和画面都走 BLE。实际常见机型会通过车端下发的 config 选择 WIFI 通道做画面传输，以获取更大带宽和更低延迟。

---

## 8. 与插件开发的关联（可选）

- 若要做“强制使用 BLE 投屏”或“强制使用 WiFi 投屏”，可考虑 Hook：  
  - `ScreenCastConfig.communicateChannel()` / `rtpChannel()` / `channel()`，或  
  - `ScreenCastConfigClient` 解析后的 `ScreenCastConfig` 的 channel 字段。  
- 若只关心“基础导航”是否可用，重点在 BLE 连接与 `DashNaviDataMessenger` 的指令；画面通道不影响导航指令。

以上分析基于对九号出行 APP 反编译代码（JADX）的阅读，关键逻辑来自 `cn.ninebot.screencast`、`cn.ninebot.mapcapture`、`cn.ninebot.device.motor.navi` 等包。

---

## 9. 投屏“画面”的真实工作逻辑（与 Hook 可行性）

### 9.1 实际逻辑（不是你理解的“录屏+叠数据”）

- **不是**：先录手机屏幕某一区域，再把预计到达等数据叠上去再发出去。
- **是**：  
  1. 根据车端下发的 **ScreenCastConfig.layout**，用 **NaviScreenCastLayoutInflater** 在内存里 **拼出一棵专门用于投屏的 View 树**。  
  2. 这棵树的组成大致是：  
     - **TYPE_BASE_MAP** → **NaviMapViewController.getView()** → 一个 **NinebotNaviView**（里面是真实导航地图，如百度地图），地图会定时截图为 Bitmap 用于锁屏/离屏渲染。  
     - **TYPE_DISTANCE_INFO** → 距离/预计到达等（DistanceInfoViewController）。  
     - **TYPE_TURNINFO** → 转向信息（TurnInfoViewController）。  
     - **TYPE_ROADLINE** → 车道线（LanesViewController）。  
     - **TYPE_CROSSIMAGE** → 路口大图（CrossImageViewController）。  
  3. 这些子 View 通过 **NaviInfo**（onNaviInfoUpdate）接收导航数据并刷新显示。  
  4. **采集**：`CaptureClient.createCodecCapture` / `createMpeg2Capture(activity, screenCapture, captureView, width, height, ...)` 里传入的 **captureView** 就是上面这棵 **根 View**（或包含它的容器）。采集端会对这个 View（或配合 MediaProjection）做绘制/编码。  
  5. 编码后的帧通过 BLE 或 WiFi 的 RTP 发到车机。

所以：**是“先按配置拼好一棵投屏专用 View 树（地图 + 预计到达等数据），再对这棵树做采集编码并发送”**；不是“先录屏再叠数据”。

### 9.2 简易导航 vs 地图导航

- **简易导航（蓝牙）**：只走 BLE 的导航指令（路名、距离、转向等），仪表自己画简易 UI，**没有**投屏画面。  
- **地图导航（投屏导航）**：上述“View 树 + 采集 + 编码 + 发送”全上，车机显示的是这棵树的画面（地图 + 预计到达等）。

### 9.3 能否 Hook 并替换/修改“地图导航”的显示内容？——可以

从实现上看，**可以**在技术上 Hook 并替换或修改投屏出去的内容，方式大致有几类：

| 方式 | 做法 | 说明 |
|------|------|------|
| **1. 替换整棵被采集的 View** | Hook **ICaptureController.replaceView(View)**：在 APP 调用 `replaceView(view)` 时，把 `view` 换成你自己构造的 View（例如你的自定义布局、自己的地图或信息页），或包一层 FrameLayout 在上面盖自己的 View。 | 编码管线不变，只是“被画出来并编码”的内容变成你的 View，**实现完全自定义显示**。 |
| **2. 替换/篡改导航数据再让原 UI 显示** | Hook **NaviScreenCastLayoutInflater.onNaviInfoUpdate(NaviInfo)** 或各 ViewController 的 **onNaviInfoUpdate**，在传入原逻辑前把 **NaviInfo** 里的距离、预计到达、路名等改成你想要的值。 | 不换 View 树，只改“地图导航”里显示的数据来源，**实现修改预计到达等数据**。 |
| **3. 只换“底图”** | Hook **NaviMapViewController.getView(Context, ViewInfo)**：让返回值为你自己的 View（例如你的地图或一张图），而不是原来的 NinebotNaviView。 | 只替换投屏里的地图层，其它距离/转向等仍用原逻辑。 |
| **4. 在创建采集时替换根 View** | Hook **CaptureClient.createCodecCapture** / **createMpeg2Capture** 或 **ScreenCastRequest.createNavigationScreenCast** 里传入的 **captureView**，在调用原方法时改为传入你提供的根 View。 | 和 1 类似，在“谁被采集”的入口做替换，**实现整屏自定义**。 |

推荐优先试 **1 或 4**（替换被采集的 View），这样“地图导航”的显示可以完全按你的要求来；若只想改预计到达等数字，用 **2** 即可。

### 9.4 实测现象：手机操作与车机是否同步

- **+/-/缩放/N 向上按钮**：点击后手机屏和车机屏都会变化，说明这些操作更新的是「投屏用」的视图或状态，两边一致。
- **手机地图双指缩放**：不会同步到车机，且几秒后手机会回弹到缩放前，像「预览」不提交。说明很可能存在 **两套视图**：一套是手机上的交互地图（可双指缩放、预览），一套是专供采集投屏的视图（受 +/-/N 等控制）。替换采集 View 时需在 **采集管线入口**（如 createCapture / CaptureService.createCodecCapture）替换，且若仍不生效需考虑 native 层是否用 MediaProjection 等其它源。

### 9.5 分辨率要求（替换投屏时）

- 采集/编码端是按 **被传入的 View 的尺寸**（或配置中的 dimensionWidth/dimensionHeight）来编码的。
- **若只做“在原画面上叠一层”**（例如用 FrameLayout 包住原 View 再在上面加 TextView）：原 View 仍是根内容，尺寸不变，**无需改分辨率**，编码器看到的是同一尺寸的根 View（我们的 wrapper 与原 View 同尺寸）。
- **若完全替换成自己的根 View**：建议让自定义 View 的宽高与原来一致（可从原 `captureView.getLayoutParams()` 或 `ScreenCastConfig` 的 dimension 读取），否则可能出现拉伸、黑边或编码异常。

### 9.6 实测结论：Hook 生效时「完全替换」会触发车机回退到简易投屏

- 用户选择**地图导航**、插件将传入的 view **完全替换**为「仅提示语」View 时：车机显示的是**简易投屏**内容（而非地图），说明 **Hook 已生效**，且管线检测到「非预期 view」后回退为简易模式（无地图画面、仅 BLE 导航数据）。
- 因此改为 **包装原 view + 叠加文案**（不替换）：保留地图 view 为子节点，仅在上层叠加「投屏导航已被Hook」，避免触发简易回退；车机应恢复显示地图并带叠加文案。
- 关键字 **「导航发起中」**：用户反馈该提示结束后即投屏成功；未在 JADX 代码搜索中命中，可能在 string 资源或其它模块，后续可据此定位投屏成功时的调用点。
- 若仍认为编码非我们传入的 View：`CaptureService.createCodecCapture` / `createMpeg2Capture` 为 **native 实现**，采集/编码在 .so 里，有可能用 MediaProjection 或其它 Surface。
- **验证**：插件会打 **logcat**，请用：
  - 投屏相关：`adb logcat -s NinebotHook/投屏:W`
  - 插件整体：`adb logcat -s NinebotHook:I` 或 `adb logcat | findstr NinebotHook`
- **MCP 源码结论（JADX）**：
  - `CaptureClient`、`CaptureService` 在 **classes17.dex**，首屏不会加载。
  - `DeviceApplication.init(Context)` 内会执行 `CaptureClient.INSTANCE.setNotificationCreator(...)`，故 **init 执行时 CaptureClient 已被加载**。
  - 插件已改为：在 **loadClass 加载到 DeviceApplication 时**挂载 `DeviceApplication.init()`，在 **init 的 afterHookedMethod** 里用 `context.getClass().getClassLoader()` 再调 `tryHookScreenCastOverlay`，从而在 CaptureClient 已存在的 ClassLoader 上挂 hook。
- **判断**：
  - 若出现 **「已挂载 DeviceApplication.init 回调」** 且 **「DeviceApplication.init 已执行，用 Application ClassLoader 挂投屏 hook」**：说明投屏 hook 在 init 后安装，再看是否出现「createCapture 已挂载」。
  - 若仍无 **「tryHookScreenCastOverlay 被调用」**：检查 LSPosed 作用域是否勾选九号、是否主进程；或当前 APK 无 `cn.ninebot.device.DeviceApplication`（需 JADX 确认类名）。
  - 若有「createCapture 已挂载」但**开始地图导航后**仍无「createCapture 已调用」：投屏流程未走 CaptureClient.createCapture，需再找入口。
- **下一步**：逆向 **capture 相关 .so**（如 libcapture、nb_screencast 等），查实际采集源（MediaProjection / Surface / View.draw）；或在 APK 内搜 **MediaProjection**、**createVirtualDisplay**、**ImageReader** 等，确认是否录屏路径。
